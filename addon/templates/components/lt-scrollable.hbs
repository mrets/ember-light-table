{{#if virtualScrollbar}}
  {{#ember-scrollable
    classNames="lt-scrollable"
    autoHide=autoHide
    horizontal=horizontal
    vertical=vertical
    scrollToY=scrollTo
    onScrollY=onScrollY
  }}
    {{yield}}
  {{/ember-scrollable}}
{{else}}
  {{yield}}
{{/if}}
